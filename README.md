# React starter project with Parcel bundler

> My custom starter React app with bootstrap and axios

## Quick Start


``` bash
# Install dependencies
npm install

# Start dev server at localhost:3000
npm start

# build project
npm run build

```